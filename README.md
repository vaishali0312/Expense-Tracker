# Expense-Tracker
A full-stack personal expense tracker built with Node.js/Express and React. Add expenses with an amount and category, view per-category totals and a grand total, and delete entries. Input is validated on both client and server. No database required — all data is stored in-memory. Run the backend with `npm start`, open `index.html` to use.
